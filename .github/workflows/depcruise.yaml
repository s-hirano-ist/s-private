name: 'depcruise'
on:
  pull_request:

jobs:
  report:
    permissions:
      pull-requests: write
    timeout-minutes: 10
    runs-on: ubuntu-24.04
    steps:
      - uses: MH4GF/dependency-cruiser-report-action@v2
        with:
            package-manager: pnpm
