# Node version SSOT: .nvmrc (mise reads it via idiomatic_version_file_enable_tools)
[settings]
idiomatic_version_file_enable_tools = ["node"]

[env]
_.file = ".env.local"

[tools]
pnpm = "11.18.0"
doppler = { version = "3.76.1", exe = "doppler" }
"npm:agent-browser" = "0.33.1"
