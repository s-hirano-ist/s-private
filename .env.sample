# Docker Compose Environment Variables
#
# compose.yaml で利用する環境変数。
# Usage: cp .env.sample .env
#
# Next.js アプリ用の環境変数は app/.env.sample を参照してください。

# --- Embedding API (HuggingFace TEI) ---
# TEI で使用するモデル ID（必須）
EMBEDDING_MODEL=intfloat/multilingual-e5-large
# ベクトルの次元数（必須、モデルに合わせて変更）
EMBEDDING_VECTOR_SIZE=1024
# Embedding 時のプレフィックス（必須）
EMBEDDING_QUERY_PREFIX=query:
EMBEDDING_PASSAGE_PREFIX=passage:

# --- Qdrant ---
# Qdrant のコレクション名（必須）
QDRANT_COLLECTION_NAME=knowledge_v2

# --- MinIO (vps profile) ---
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_BUCKET_NAME=

# --- Cloudflare Tunnel (vps profile) ---
CLOUDFLARE_TUNNEL_TOKEN=
